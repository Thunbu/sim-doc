import {MessageStoreTypes} from "../Types/message.t";
import {DefaultMessageList} from "../../dafault.data";

export const MessageStoreData: MessageStoreTypes = {
    MessageMap: DefaultMessageList,
    ChatMessageList: {}
}
