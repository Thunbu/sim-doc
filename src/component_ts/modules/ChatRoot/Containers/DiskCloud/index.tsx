import * as React from "react";
import {connect} from "react-redux";

class DiskCloud extends React.Component<any, any> {
    render() {
        return (
            <div></div>
        );
    }
}
export default connect()(DiskCloud);
